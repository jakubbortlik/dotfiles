#!/bin/bash
# Open all files that match a parameter in LibreOffice Writer.
# If no file names are given, open a new file.

if [ "$@" ]
then
	for var in "$@"
	do
		if [ -e "$var" ]
		then
			lowriter "$var" &> /dev/null &
		else
			echo "File \"$var\" does not exist."
		fi
	done
else
	lowriter &> /dev/null &
fi

exit
